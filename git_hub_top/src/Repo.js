class Repo {
  constructor(total, name, desc, created, address) {
    this.state = {
        total: this.total,
        name: this.name,
        desc: this.desc,
        created: this.created,
        address: this.address
    };
  }
}

export default Repo;